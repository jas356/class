/*
objective - create a function that converts a string to a freiendly sentence

string method tooLowerCase(), toUpperCase(), trim()
*/

function converterSentence(text) {
    const upperText1 = text.toLowerCase();
    const upperText2 = upperText [0].toUppercase();
    const finalText = upperText2 + upperText.substring(1);
    return finalText

}

const sentence = "type better please"
const result = converterSentence


